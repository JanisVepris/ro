{% extends '@Admin/admin_base.html.twig' %}
{% from '@Admin/AdminLayout/macros.html.twig' import main_toolbar %}

{% set tabTitle = event.title ~ ' - Tekstai' %}

{% block content %}
    <div class="text-center">
        <h2>MIDI Roko Operos "{{ event.title }}" Dainų Tekstai</h2>
    </div>
    <div class="col-md-12">
        {% if not event.hasLyrics %}
            {{ main_toolbar(path('admin_lyrics_create', {'eventId': event.id})) }}
        {% else %}
            {% include 'AdminBundle:AdminLyrics/partial:__lyricList.html.twig' with {'lyrics': lyrics} %}
        {% endif %}
    </div>
{% endblock %}
